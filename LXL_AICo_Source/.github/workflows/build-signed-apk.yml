name: Build Signed Release APK

on:
  workflow_dispatch:

jobs:
  build-signed:
    name: Build Signed Release APK
    runs-on: ubuntu-latest
    timeout-minutes: 60

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Cache Gradle dependencies
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle.kts', '**/gradle-wrapper.properties') }}
          restore-keys: |
            ${{ runner.os }}-gradle-

      - name: Grant execute permission for gradlew
        working-directory: ./android
        run: chmod +x gradlew

      - name: Build Debug APK first (verify compilation)
        working-directory: ./android
        run: ./gradlew assembleDebug --no-daemon --stacktrace

      - name: Restore Keystore from Secret
        run: |
          echo "${{ secrets.KEYSTORE_BASE64 }}" | base64 -d > /tmp/stockadvisor.keystore
          ls -la /tmp/stockadvisor.keystore

      - name: Build Signed Release APK
        working-directory: ./android
        run: |
          echo "storeFile=/tmp/stockadvisor.keystore" > signing.properties
          echo "storePassword=${{ secrets.KEYSTORE_PASSWORD }}" >> signing.properties
          echo "keyAlias=stockadvisor" >> signing.properties
          echo "keyPassword=${{ secrets.KEY_PASSWORD }}" >> signing.properties
          echo "storeType=PKCS12" >> signing.properties
          cat signing.properties
          ls -la /tmp/stockadvisor.keystore
          ./gradlew assembleRelease --no-daemon --stacktrace

      - name: Upload Signed APK
        uses: actions/upload-artifact@v4
        with:
          name: stockadvisor-release-signed
          path: android/app/build/outputs/apk/release/app-release.apk
          retention-days: 90
